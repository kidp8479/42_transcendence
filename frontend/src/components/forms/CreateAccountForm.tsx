/**
 * Create account form component.
 * Handles user registration input including email, password,
 * and campus selection, along with optional OAuth sign-up methods.
 *
 * Includes:
 * - Email field
 * - Password field
 * - Campus selection field
 * - OAuth sign-up options
 * - Create account submit button
 * - Sign in link (switches to login view/modal)
 *
 * This component is responsible for the UI and interaction logic
 * of the registration process only.
 *
 * It is typically rendered inside AuthModal.
 *
 * It does NOT handle:
 * - Modal visibility (handled by ModalLayer/ModalProvider)
 * - Routing or navigation logic
 */
