/**
 * Sign in form component.
 * Handles user authentication input including email/password
 * and optional OAuth sign-in methods.
 *
 * Includes secondary actions:
 * - Forgot password link (opens forgot password modal)
 * - Sign up link (switches to registration view/modal)
 *
 * This component is responsible for the UI and interaction logic
 * of the sign-in process only.
 *
 * It is typically rendered inside AuthModal.
 *
 * It does NOT handle:
 * - Modal visibility (handled by ModalLayer/ModalProvider)
 * - Routing or navigation logic
 */
