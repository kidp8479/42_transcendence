/**
 * Forgot password modal.
 * Handles the UI for initiating a password reset flow.
 *
 * This modal is rendered through ModalLayer when the
 * active modal state is set to 'forgotPassword'.
 *
 * It is part of the authentication feature domain and
 * should be used only within the global modal system.
 */