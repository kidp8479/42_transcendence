// THIS FILE IS A PLACEHOLDER - seed logic to be implemented later
// seed = inject demo data directly into the database at startup, for dev and demo purposes
// run with: npx prisma db seed (configured in package.json under "prisma.seed", requires ts-node - already installed)

// what this file will do:
// 1. create a demo project (ex: "ft_transcendence") with a description and members
// 2. pre-fill the Discovery tab with default categories so the user has a starting point
//    they can rename, delete, or add more - these are just suggestions, not locked in
//    ex: "Research", "Architecture", "Design", "References", "Meeting Notes"
//    each with a default icon and color, and a few empty items inside

// note: everything else (form placeholders, empty state messages, input hints)
// is handled in the frontend with the HTML "placeholder" attribute - no DB needed for that
