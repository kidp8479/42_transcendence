# The backend exposes REST endpoints that mutate or remove resources. Keep the
# CRS default allowlist narrow while admitting only the methods the API uses.
SecAction \
    "id:1000001,\
    phase:1,\
    pass,\
    t:none,\
    nolog,\
    setvar:'tx.allowed_methods=GET HEAD POST OPTIONS PATCH DELETE'"
