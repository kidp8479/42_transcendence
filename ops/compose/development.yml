services:
  nginx:
    ports: !override
      - "${APP_INGRESS_BIND_ADDRESS:-127.0.0.1}:${APP_INGRESS_HTTP_PORT:-8081}:8080"
      - "${APP_INGRESS_BIND_ADDRESS:-127.0.0.1}:${APP_INGRESS_HTTPS_PORT:-8444}:8443"
  db:
    ports: !override []
  vault:
    ports: !override []
  rustfs:
    ports: !override []
